<?xml version="1.0" encoding="UTF-8"?>
<!-- Structure only. Every colour, size and spacing comes from the shared
     design system (theme/), which main.py applies to the whole application.

     The minimum size is what the mission page needs to draw itself: the live
     picture and the detection crop are both always on the screen and neither
     scrolls, so together they set the floor under the window's height. -->
<ui version="4.0">
 <class>SuasMainWindow</class>
 <widget class="QMainWindow" name="SuasMainWindow">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>1600</width>
    <height>900</height>
   </rect>
  </property>
  <property name="minimumSize">
   <size>
    <width>1280</width>
    <height>880</height>
   </size>
  </property>
  <property name="windowTitle">
   <string>MainWindow</string>
  </property>
  <widget class="QWidget" name="centralwidget">
   <layout class="QVBoxLayout" name="rootlayout"/>
  </widget>
  <widget class="QStatusBar" name="statusbar"/>
 </widget>
 <resources/>
 <connections/>
</ui>
